/**
 * Ausgelagert aus lib/actions/memories.ts, da 'use server'-Dateien nur async
 * Funktionen exportieren dürfen (keine Konstanten/Werte).
 */
/** §"Maximal etwa 25 dauerhafte Bilder je Reise" (Galerie-Sprint, vorher 30). */
export const MAX_SELECTED_PHOTOS_PER_TRIP = 25
